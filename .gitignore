# --- copyrighted material and anything derived from it: never commit, never publish ---
sources/
images/
docs/manuals/
docs/topics/
docs/devices/
docs/inventory.md
docs/index.md
docs/llms.txt

# --- generated config and local-only additions ---
mkdocs.yml
mkdocs.local.yml
catalog-info.yaml
tools/local_extras.py

# --- environment and build output ---
.venv/
.venv-graph/
site/
__pycache__/
*.pyc

# --- graph build: full output includes nodes derived from manual text, so it stays local.
#     Only graph/public/ (built from committed data, no manual text) may be committed. ---
graph/out/
spark-warehouse/
metastore_db/

# --- anything private lives outside the repository; these patterns are a second safety net ---
private/
*.private.md

# --- generated public site (rebuilt by CI). Anchored to the root: graph/public/ is a committed snapshot ---
/public/
